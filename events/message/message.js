/*      Mise en place des variables constantes      */
const Discord = require("discord.js"),
      client = new Discord.Client();

const fs = require("fs");

const mysql = require("mysql2");

/*      Creation de l'event message     */
module.exports = async (client, sqlconnection, message) => {

};